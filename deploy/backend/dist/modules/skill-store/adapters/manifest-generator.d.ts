import { AnalysisResult, SkillManifest } from './skill-adapter.interface';
export declare class ManifestGenerator {
    parseSkillMd(content: string, localPath: string, analysis: AnalysisResult): SkillManifest;
    autoGenerateDefaults(localPath: string, analysis: AnalysisResult): Partial<SkillManifest>;
    private deriveRuntimeType;
    private deriveEntryPoint;
    private deriveNameFromDir;
    private guessCategory;
    private extractFirstH1;
    private extractFirstParagraph;
    private normalizeTriggerKeywords;
}
