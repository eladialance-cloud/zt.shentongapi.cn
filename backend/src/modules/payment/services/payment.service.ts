import {  Injectable,  NotFoundException,  BadRequestException,  Logger,} from '@nestjs/common';import { InjectRepository, InjectDataSource } from '@nestjs/typeorm';import { Repository, DataSource } from 'typeorm';import * as crypto from 'crypto';import { ConfigService } from '@nestjs/config';import * as fs from 'fs';import WechatPay from 'wechatpay-node-v3';import { AlipaySdk } from 'alipay-sdk';import Stripe from 'stripe';import { PaymentRecordEntity } from '../entities/payment-record.entity';import { RechargeOrderEntity } from '../entities/recharge-order.entity';import { MembershipPlanEntity } from '../entities/membership-plan.entity';import { CreditAccountEntity } from '../../credits/entities/credit-account.entity';import { CreditTransactionEntity } from '../../credits/entities/credit-transaction.entity';import { CreditsService } from '../../credits/services/credits.service';import { RedisService } from '../../../common/services/redis.service';import { BusinessException } from '../../../common/exceptions/business.exception';import { ErrorCode } from '../../../common/constants/error.constant';import { SystemConfigEntity } from '../../admin-system/entities/system-config.entity';import { EncryptionService } from '../../../common/services/encryption.service';type PaymentChannel = 'wechat' | 'alipay' | 'stripe';/** * 鏀粯鏈嶅姟 * 鏁版嵁鍚堝悓鐪熸簮锛歍ask 30 - 绉垎鍏呭€间笌鏀粯 * 娴佺▼锛氬垱寤鸿鍗?鈫?鍙戣捣鏀粯 鈫?鍥炶皟楠岀 鈫?绉垎鍏ヨ处 * * 浼氬憳濂楅鍒楄〃璧?Redis 缂撳瓨锛圚-13锛夛紝鍑忓皯鏁版嵁搴撳帇鍔涖€? */@Injectable()export class PaymentService {  private readonly logger = new Logger(PaymentService.name);  private static readonly PLANS_CACHE_KEY = 'cache:plans:list';  private static readonly PLANS_CACHE_TTL = 3600; // 1 灏忔椂  private wechatPay: WechatPay | null = null;  private alipaySdk: AlipaySdk | null = null;  private stripeClient: Stripe | null = null;  constructor(    @InjectRepository(PaymentRecordEntity)    private paymentRepo: Repository<PaymentRecordEntity>,    @InjectRepository(RechargeOrderEntity)    private orderRepo: Repository<RechargeOrderEntity>,    @InjectRepository(MembershipPlanEntity)    private planRepo: Repository<MembershipPlanEntity>,    @InjectRepository(SystemConfigEntity)    private configRepo: Repository<SystemConfigEntity>,    private creditsService: CreditsService,    private readonly redis: RedisService,    private readonly config: ConfigService,    private readonly encryptionService: EncryptionService,    @InjectDataSource() private dataSource: DataSource,  ) {}  // ============ 鍏呭€兼祦绋?============  /** 鍒涘缓鍏呭€艰鍗?*/  async createRechargeOrder(    userId: number,    packageId: number | null,    credits: number,    amount: number,    channel: PaymentChannel,  ): Promise<RechargeOrderEntity> {    if (amount <= 0) {      BusinessException.throw(ErrorCode.VALIDATION_FAILED, '鍏呭€奸噾棰濆繀椤诲ぇ浜?0');    }    if (credits <= 0) {      BusinessException.throw(        ErrorCode.VALIDATION_FAILED,        '鍏呭€肩Н鍒嗘暟閲忓繀椤诲ぇ浜?0',      );    }    const orderNo = this.generateOrderNo();    const order = this.orderRepo.create({      orderNo,      userId,      packageId: packageId ?? undefined,      credits,      amount,      status: 'pending',      paymentChannel: channel,    });    return this.orderRepo.save(order);  }  /** 鍙戣捣鏀粯 */  async initiatePayment(    orderNo: string,    channel: PaymentChannel,    subMethod: string,  ): Promise<{    paymentId: number;    orderNo: string;    payParams: Record<string, unknown>;  }> {    const order = await this.orderRepo.findOne({ where: { orderNo } });    if (!order) throw new NotFoundException('璁㈠崟涓嶅瓨鍦?);    if (order.status !== 'pending') {      throw new BadRequestException('璁㈠崟鐘舵€佷笉鍏佽鏀粯');    }    // 鍒涘缓鏀粯璁板綍    const payment = this.paymentRepo.create({      userId: order.userId,      orderNo: order.orderNo,      channel,      subMethod: subMethod as PaymentRecordEntity['subMethod'],      amount: order.amount,      currency: 'CNY',      status: 'pending',      description: `鍏呭€?${order.credits} 绉垎`,    });    const saved = await this.paymentRepo.save(payment);    // 鐢熸垚鏀粯鍙傛暟锛堝鎺ュ悇鏀粯娓犻亾 SDK锛?    const payParams = await this.generatePayParams(channel, subMethod, {      orderNo: order.orderNo,      amount: Number(order.amount),      description: payment.description || '',    });    saved.payParams = payParams;    await this.paymentRepo.save(saved);    // 鍏宠仈璁㈠崟涓庢敮浠樿褰?    order.paymentRecordId = saved.id;    await this.orderRepo.save(order);    return {      paymentId: saved.id,      orderNo: order.orderNo,      payParams,    };  }  /** 鏀粯鍥炶皟锛堢敱鏀粯娓犻亾璋冪敤锛?*/  async handlePaymentCallback(    channel: string,    callbackData: Record<string, unknown>,  ): Promise<{ success: boolean; orderNo?: string }> {    // 1. 楠岃瘉绛惧悕锛堝悇娓犻亾涓嶅悓锛?    const verified = await this.verifyCallbackSignature(channel, callbackData);    if (!verified) {      return { success: false };    }    // 2. 鎻愬彇鍏抽敭淇℃伅    const orderNo = callbackData.out_trade_no as string;    const txnId = callbackData.transaction_id as string;    // 寰俊 V3 鍥炶皟瑙ｅ瘑鍚庨噾棰濆湪 amount.total锛堝崟浣嶏細鍒嗭級锛岄渶闄や互 100 杞负鍏?    let amount: number;    if (channel === 'wechat' && callbackData.amount && typeof callbackData.amount === 'object') {      amount = Number((callbackData.amount as any).total || 0) / 100;    } else {      amount = Number(callbackData.total_amount || callbackData.amount || 0);    }    if (!orderNo) {      this.logger.warn('鍥炶皟缂哄皯 out_trade_no');      return { success: false };    }    // 3. 骞傜瓑澶勭悊锛堜簨鍔?+鎮茶閿侊級    return this.dataSource.transaction(async (manager) => {      const order = await manager.findOne(RechargeOrderEntity, {        where: { orderNo },        lock: { mode: 'pessimistic_write' },      });      if (!order) throw new NotFoundException('璁㈠崟涓嶅瓨鍦?);      // 宸叉敮浠橈紝骞傜瓑杩斿洖      if (order.status === 'paid') {        return { success: true, orderNo };      }      // 4. 楠岃瘉閲戦锛坥rder.amount 涓?decimal(10,2)锛孴ypeORM 鍙兘杩斿洖 string锛岄渶鏄惧紡 Number 杞崲锛?      const orderAmount = Number(order.amount);      if (Math.abs(orderAmount - amount) > 0.01) {        throw new BadRequestException('閲戦涓嶅尮閰?);      }      // 5. 鏇存柊璁㈠崟      order.status = 'paid';      await manager.save(order);      // 6. 鏇存柊鏀粯璁板綍      await manager.update(        PaymentRecordEntity,        { orderNo },        {          status: 'paid',          paymentTxnId: txnId,          paidAt: new Date(),          callbackRaw: callbackData,        } as any,      );      // 7. 绉垎鍏ヨ处      await this.creditsService.rechargeCredits(        order.userId,        order.credits,        order.orderNo,        `鍏呭€?${order.credits} 绉垎`,      );      return { success: true, orderNo };    });  }  /** 鏌ヨ璁㈠崟鐘舵€?*/  async getOrderStatus(orderNo: string, userId: number) {    const order = await this.orderRepo.findOne({      where: { orderNo, userId },    });    if (!order) throw new NotFoundException('璁㈠崟涓嶅瓨鍦?);    return {      orderNo: order.orderNo,      status: order.status,      credits: order.credits,      amount: order.amount,      createdAt: order.createdAt,      paidAt: order.updatedAt,    };  }  /** 鐢ㄦ埛鍏呭€艰鍗曞垪琛?*/  async getUserOrders(userId: number, page = 1, pageSize = 20) {    const [list, total] = await this.orderRepo.findAndCount({      where: { userId },      order: { createdAt: 'DESC' },      skip: (page - 1) * pageSize,      take: pageSize,    });    return {      list,      total,      page,      pageSize,      totalPages: Math.ceil(total / pageSize) || 0,    };  }  // ============ 浼氬憳濂楅 ============  /** 鑾峰彇濂楅鍒楄〃锛堝甫 Redis 缂撳瓨锛孴TL 1 灏忔椂锛?*/  async getPlans(): Promise<MembershipPlanEntity[]> {    const cached = await this.redis.get(PaymentService.PLANS_CACHE_KEY);    if (cached) {      try {        return JSON.parse(cached) as MembershipPlanEntity[];      } catch {        // 缂撳瓨鎹熷潖锛岀户缁煡 DB      }    }    const result = await this.planRepo.find({      where: { isActive: true },      order: { price: 'ASC' },    });    await this.redis.set(      PaymentService.PLANS_CACHE_KEY,      JSON.stringify(result),      PaymentService.PLANS_CACHE_TTL,    );    return result;  }  /** 鑾峰彇鍏ㄩ儴濂楅锛堢鐞嗙锛屽惈鍋滅敤锛?*/  async getAllPlans(): Promise<MembershipPlanEntity[]> {    return this.planRepo.find({      order: { level: 'ASC', price: 'ASC' },    });  }  /** 鍒涘缓濂楅锛堢鐞嗗憳锛?*/  async createPlan(    data: Partial<MembershipPlanEntity>,  ): Promise<MembershipPlanEntity> {    const saved = await this.planRepo.save(this.planRepo.create(data));    await this.redis.del(PaymentService.PLANS_CACHE_KEY);    return saved;  }  /** 鏇存柊濂楅锛堢鐞嗗憳锛?*/  async updatePlan(    id: number,    data: Partial<MembershipPlanEntity>,  ): Promise<void> {    await this.planRepo.update(id, data);    await this.redis.del(PaymentService.PLANS_CACHE_KEY);  }  /** 鍒犻櫎濂楅锛堢鐞嗗憳锛?*/  async deletePlan(id: number): Promise<void> {    await this.planRepo.delete(id);    await this.redis.del(PaymentService.PLANS_CACHE_KEY);  }  // ============ 閫€娆?============  /** 璁㈠崟閫€娆?*/  async refundOrder(    orderId: number,    adminId: number,    reason: string,  ): Promise<{ success: boolean }> {    const order = await this.orderRepo.findOne({ where: { id: orderId } });    if (!order) throw new NotFoundException('璁㈠崟涓嶅瓨鍦?);    if (order.status !== 'paid') {      throw new BadRequestException('鍙兘閫€娆惧凡鏀粯鐨勮鍗?);    }    return this.dataSource.transaction(async (manager) => {      // 鏇存柊璁㈠崟鐘舵€?      order.status = 'refunded';      await manager.save(order);      // 鏇存柊鏀粯璁板綍      await manager.update(        PaymentRecordEntity,        { orderNo: order.orderNo },        {          status: 'refunded',          refundAmount: order.amount,          refundedAt: new Date(),        },      );      // 閫€娆炬墸鍑忕Н鍒?鈥?鍏佽浣欓鍙樿礋锛堜笌 adminAdjust 涓嶅悓锛?      const account = await this.creditsService.getOrCreateAccount(order.userId);      const balanceBefore = account.balance;      const balanceAfter = balanceBefore - order.credits;      await manager.update(        CreditAccountEntity,        { id: account.id },        {          balance: balanceAfter,          version: account.version + 1,          totalConsumed: account.totalConsumed + order.credits,        },      );      const txn = manager.getRepository(CreditTransactionEntity).create({        userId: order.userId,        type: 'refund_deduct',        amount: -order.credits,        balanceBefore,        balanceAfter,        source: 'refund',        sourceId: `refund_${order.id}`,        adminId,        remark: `璁㈠崟閫€娆?${order.orderNo}: ${reason}`,      });      await manager.save(txn);      return { success: true };    });  }  // ============ 鏀粯閰嶇疆锛堟暟鎹簱浼樺厛锛岀幆澧冨彉閲?fallback锛?============  /** 浠?system_config 琛ㄨ鍙?payment section 閰嶇疆 */  /** 鑾峰彇鏀粯閰嶇疆锛堣嚜鍔ㄨВ瀵嗘晱鎰熷瓧娈碉級 - P0 淇 */  private async loadPaymentConfig(): Promise<Record<string, any>> {    const row = await this.configRepo.findOne({ where: { section: 'payment' } });    if (row && row.configValue) {      const config = row.configValue as Record<string, any>;      // 灏濊瘯瑙ｅ瘑宸茬煡鏁忔劅瀛楁锛圓ES-256-GCM 鏍煎紡锛歩v:authTag:ciphertext锛?      try {        if (config.wechat?.apiV3Key && String(config.wechat.apiV3Key).split(':').length === 3) {          config.wechat.apiV3Key = this.encryptionService.decryptAes(config.wechat.apiV3Key);        }        if (config.alipay?.privateKey && String(config.alipay.privateKey).split(':').length === 3) {          config.alipay.privateKey = this.encryptionService.decryptAes(config.alipay.privateKey);        }        if (config.stripe?.secretKey && String(config.stripe.secretKey).split(':').length === 3) {          config.stripe.secretKey = this.encryptionService.decryptAes(config.stripe.secretKey);        }        if (config.stripe?.webhookSecret && String(config.stripe.webhookSecret).split(':').length === 3) {          config.stripe.webhookSecret = this.encryptionService.decryptAes(config.stripe.webhookSecret);        }      } catch {        this.logger.warn('鏀粯閰嶇疆瑙ｅ瘑澶辫触锛屽彲鑳戒负鏄庢枃鏍煎紡');      }      return config;    }    return {};  }  /** 鑾峰彇寰俊鏀粯閰嶇疆锛堟暟鎹簱浼樺厛锛岀幆澧冨彉閲?fallback锛?*/  private async getWechatConfig(): Promise<{    appId: string;    mchId: string;    apiV3Key: string;    serialNo: string;    privateKeyPath: string;    publicKeyPath: string;    notifyUrl: string;    callbackIps: string;    enabled: boolean;  }> {    const dbConfig = await this.loadPaymentConfig();    const wechat = (dbConfig.wechat || {}) as Record<string, any>;    return {      appId: wechat.appId || this.config.get<string>('WECHAT_APP_ID') || '',      mchId: wechat.mchId || this.config.get<string>('WECHAT_MCH_ID') || '',      apiV3Key: wechat.apiV3Key || this.config.get<string>('WECHAT_API_V3_KEY') || '',      serialNo: wechat.serialNo || this.config.get<string>('WECHAT_SERIAL_NO') || '',      privateKeyPath: wechat.privateKeyPath || this.config.get<string>('WECHAT_PRIVATE_KEY_PATH') || '',      publicKeyPath: wechat.publicKeyPath || this.config.get<string>('WECHAT_PUBLIC_KEY_PATH') || '',      notifyUrl: wechat.notifyUrl || this.config.get<string>('WECHAT_NOTIFY_URL') || '',      callbackIps: wechat.callbackIps || this.config.get<string>('WECHAT_CALLBACK_IPS') || '',      enabled: wechat.enabled ?? !!this.config.get<string>('WECHAT_APP_ID'),    };  }  /** 鑾峰彇鏀粯瀹濋厤缃?*/  private async getAlipayConfig(): Promise<{    appId: string;    privateKey: string;    publicKey: string;    notifyUrl: string;    enabled: boolean;  }> {    const dbConfig = await this.loadPaymentConfig();    const alipay = (dbConfig.alipay || {}) as Record<string, any>;    return {      appId: alipay.appId || this.config.get<string>('ALIPAY_APP_ID') || '',      privateKey: alipay.privateKey || this.config.get<string>('ALIPAY_PRIVATE_KEY') || '',      publicKey: alipay.publicKey || this.config.get<string>('ALIPAY_PUBLIC_KEY') || '',      notifyUrl: alipay.notifyUrl || this.config.get<string>('ALIPAY_NOTIFY_URL') || '',      enabled: alipay.enabled ?? !!this.config.get<string>('ALIPAY_APP_ID'),    };  }  /** 鑾峰彇 Stripe 閰嶇疆 */  private async getStripeConfig(): Promise<{    secretKey: string;    webhookSecret: string;    enabled: boolean;  }> {    const dbConfig = await this.loadPaymentConfig();    const stripe = (dbConfig.stripe || {}) as Record<string, any>;    return {      secretKey: stripe.secretKey || this.config.get<string>('STRIPE_SECRET_KEY') || '',      webhookSecret: stripe.webhookSecret || this.config.get<string>('STRIPE_WEBHOOK_SECRET') || '',      enabled: stripe.enabled ?? !!this.config.get<string>('STRIPE_SECRET_KEY'),    };  }  // ============ 杈呭姪鏂规硶 ============  private generateOrderNo(): string {    const date = new Date();    const dateStr =      date.getFullYear().toString() +      String(date.getMonth() + 1).padStart(2, '0') +      String(date.getDate()).padStart(2, '0');    const random = crypto.randomBytes(8).toString('hex').toUpperCase();    return `RO${dateStr}${random}`;  }  private async generatePayParams(    channel: string,    subMethod: string,    params: { orderNo: string; amount: number; description: string },  ): Promise<Record<string, unknown>> {    switch (channel) {      case 'wechat':        return this.generateWechatPayParams(params);      case 'alipay':        return this.generateAlipayParams(params);      case 'stripe':        return this.generateStripeParams(params);      default:        throw new BadRequestException(`涓嶆敮鎸佺殑鏀粯娓犻亾: ${channel}`);    }  }  // ============ 寰俊鏀粯 V3 ============  private async initWechatPay(): Promise<void> {    if (this.wechatPay) return;    const cfg = await this.getWechatConfig();    if (!cfg.enabled || !cfg.appId || !cfg.mchId || !cfg.apiV3Key || !cfg.serialNo || !cfg.privateKeyPath) {      this.logger.warn('寰俊鏀粯鍑瘉鏈厤缃紝寰俊娓犻亾宸茬鐢?);      return;    }    try {      const privateKey = fs.readFileSync(cfg.privateKeyPath);      // 骞冲彴璇佷功鐢ㄤ簬鍥炶皟 RSA-SHA256 楠岀銆傝嫢鏈厤缃矾寰勶紝鍒欎紶绌?Buffer銆?      const publicKey = cfg.publicKeyPath        ? fs.readFileSync(cfg.publicKeyPath)        : Buffer.from('');      this.wechatPay = new WechatPay({        appid: cfg.appId,        mchid: cfg.mchId,        privateKey,        publicKey,        key: cfg.apiV3Key,        serial_no: cfg.serialNo,      });      this.logger.log('寰俊鏀粯 V3 瀹㈡埛绔垵濮嬪寲鎴愬姛');    } catch (err) {      this.logger.error(`寰俊鏀粯鍒濆鍖栧け璐? ${(err as Error).message}`);    }  }  private async generateWechatPayParams(    params: { orderNo: string; amount: number; description: string },  ): Promise<Record<string, unknown>> {    if (!this.wechatPay) {      await this.initWechatPay();    }    if (!this.wechatPay) {      BusinessException.throw(        ErrorCode.PAYMENT_CHANNEL_DISABLED,        '寰俊鏀粯娓犻亾鏈惎鐢?,      );    }    const cfg = await this.getWechatConfig();    const notifyUrl = cfg.notifyUrl;    if (!notifyUrl) {      BusinessException.throw(        ErrorCode.PAYMENT_CHANNEL_DISABLED,        '寰俊鏀粯鍥炶皟鍦板潃鏈厤缃?,      );    }    // wechatpay-node-v3 鐨?transactions_native 杩斿洖 Output锛屽疄闄呬笟鍔℃暟鎹湪 data 瀛楁涓?    const result = await this.wechatPay.transactions_native({      description: params.description,      out_trade_no: params.orderNo,      notify_url: notifyUrl,      amount: {        total: Math.round(params.amount * 100), // 寰俊鏀粯閲戦鍗曚綅涓哄垎        currency: 'CNY',      },    });    if (result.status !== 200 || result.error) {      this.logger.error(        `寰俊鏀粯涓嬪崟澶辫触: status=${result.status}, error=${JSON.stringify(result.error)}`,      );      BusinessException.throw(        ErrorCode.PAYMENT_CHANNEL_DISABLED,        `寰俊鏀粯涓嬪崟澶辫触: ${result.errRaw?.message || '鏈煡閿欒'}`,      );    }    const data = (result.data || {}) as Record<string, unknown>;    return {      code_url: data.code_url,      prepay_id: data.prepay_id,    };  }  // ============ 鏀粯瀹濆綋闈粯 ============  private async initAlipay(): Promise<void> {    if (this.alipaySdk) return;    const cfg = await this.getAlipayConfig();    if (!cfg.enabled || !cfg.appId || !cfg.privateKey || !cfg.publicKey) {      this.logger.warn('鏀粯瀹濆嚟璇佹湭閰嶇疆锛屾敮浠樺疂娓犻亾宸茬鐢?);      return;    }    try {      this.alipaySdk = new AlipaySdk({        appId: cfg.appId,        privateKey: cfg.privateKey,        alipayPublicKey: cfg.publicKey,        signType: 'RSA2',      });      this.logger.log('鏀粯瀹濆鎴风鍒濆鍖栨垚鍔?);    } catch (err) {      this.logger.error(`鏀粯瀹濆垵濮嬪寲澶辫触: ${(err as Error).message}`);    }  }  private async generateAlipayParams(    params: { orderNo: string; amount: number; description: string },  ): Promise<Record<string, unknown>> {    if (!this.alipaySdk) {      await this.initAlipay();    }    if (!this.alipaySdk) {      BusinessException.throw(        ErrorCode.PAYMENT_CHANNEL_DISABLED,        '鏀粯瀹濇笭閬撴湭鍚敤',      );    }    const cfg = await this.getAlipayConfig();    const notifyUrl = cfg.notifyUrl;    // alipay-sdk v4 鐨?exec 鏂规硶锛歮ethod 鍚?+ IRequestParams    // 杩斿洖 AlipaySdkCommonResult锛宑ode=10000 琛ㄧず鎴愬姛锛屼笟鍔″瓧娈电洿鎺ユ寕鍦ㄧ粨鏋滀笂锛堥粯璁ら┘宄帮級    const result = await this.alipaySdk.exec('alipay.trade.precreate', {      bizContent: {        out_trade_no: params.orderNo,        total_amount: params.amount.toFixed(2),        subject: params.description,      },      notify_url: notifyUrl,    });    if (result.code !== '10000') {      this.logger.error(        `鏀粯瀹濋鍒涘缓澶辫触: code=${result.code}, msg=${result.msg}, sub_code=${result.sub_code}, sub_msg=${result.sub_msg}`,      );      BusinessException.throw(        ErrorCode.PAYMENT_CHANNEL_DISABLED,        `鏀粯瀹濋鍒涘缓澶辫触: ${result.sub_msg || result.msg}`,      );    }    return {      qr_code: result.qrCode || result.qr_code,      out_trade_no: params.orderNo,    };  }  // ============ Stripe PaymentIntent ============  private async initStripe(): Promise<void> {    if (this.stripeClient) return;    const cfg = await this.getStripeConfig();    if (!cfg.enabled || !cfg.secretKey) {      this.logger.warn('Stripe 鍑瘉鏈厤缃紝Stripe 娓犻亾宸茬鐢?);      return;    }    try {      // stripe@22 鐨?apiVersion 绫诲瀷涓烘渶鏂扮増鏈瓧闈㈤噺锛?026-06-24.dahlia锛夛紱      // 杩欓噷閿佸畾 2024-06-20 浠ュ尮閰嶈处鎴烽厤缃紝鐢?as any 缁曡繃瀛楅潰閲忕被鍨嬫牎楠?      this.stripeClient = new Stripe(cfg.secretKey, {        apiVersion: '2024-06-20' as any,      });      this.logger.log('Stripe 瀹㈡埛绔垵濮嬪寲鎴愬姛');    } catch (err) {      this.logger.error(`Stripe 鍒濆鍖栧け璐? ${(err as Error).message}`);    }  }  private async generateStripeParams(    params: { orderNo: string; amount: number; description: string },  ): Promise<Record<string, unknown>> {    if (!this.stripeClient) {      await this.initStripe();    }    if (!this.stripeClient) {      BusinessException.throw(        ErrorCode.PAYMENT_CHANNEL_DISABLED,        'Stripe 娓犻亾鏈惎鐢?,      );    }    const intent = await this.stripeClient.paymentIntents.create({      amount: Math.round(params.amount * 100), // Stripe 閲戦鍗曚綅涓哄垎      currency: 'cny',      metadata: { order_no: params.orderNo },      description: params.description,    });    return {      client_secret: intent.client_secret,      payment_intent_id: intent.id,    };  }  private async verifyCallbackSignature(    channel: string,    data: Record<string, unknown>,  ): Promise<boolean> {    /**     * 鍚勬敮浠樻笭閬撳洖璋冮獙绛?     * 瀵嗛挜閫氳繃鐜鍙橀噺娉ㄥ叆锛歐ECHAT_API_V3_KEY / ALIPAY_PUBLIC_KEY / STRIPE_WEBHOOK_SECRET     */    try {      if (channel === 'wechat') {        // 寰俊鏀粯 V3 鍥炶皟楠岀锛歊SA-SHA256 骞冲彴璇佷功楠岀 + AES-256-GCM 瑙ｅ瘑        const cfg = await this.getWechatConfig();        // IP 鐧藉悕鍗曟牎楠岋紙寮哄埗锛夆€斺€旂敓浜х幆澧冨繀椤婚厤缃井淇℃敮浠樺洖璋僆P鐧藉悕鍗?        // 鏈厤缃櫧鍚嶅崟鏃舵嫆缁濇墍鏈夊洖璋冿紙闃蹭吉閫犲洖璋冩敾鍑伙級        if (!cfg.callbackIps) {          this.logger.error('寰俊鏀粯鍥炶皟 IP 鐧藉悕鍗曟湭閰嶇疆锛屾嫆缁濆洖璋?);          return false;        }        const clientIP = (data as any)['client-ip'] as string;        if (!clientIP) {          this.logger.error('寰俊鏀粯鍥炶皟缂哄皯 client-ip锛屾嫆缁濆洖璋?);          return false;        }        const ipRanges = cfg.callbackIps.split(',').map((s) => s.trim());        const isAllowed = ipRanges.some((range) => clientIP.startsWith(range));        if (!isAllowed) {          this.logger.error(`寰俊鏀粯鍥炶皟 IP 涓嶅湪鐧藉悕鍗? ${clientIP}`);          return false;        }        const signature = data['wechatpay-signature'] as string;        const serial = data['wechatpay-serial'] as string;        const nonce = data['wechatpay-nonce'] as string;        const timestamp = data['wechatpay-timestamp'] as string;        const body = data.body as Record<string, unknown>;        if (!signature || !nonce || !timestamp || !body) {          this.logger.error('寰俊鏀粯 V3 鍥炶皟缂哄皯蹇呰鐨勫ご淇℃伅鎴?body');          return false;        }        // 闃查噸鏀撅細鏃堕棿鎴宠秴杩?5 鍒嗛挓鎷掔粷        const age = Math.floor(Date.now() / 1000) - Number(timestamp);        if (age > 300) {          this.logger.warn(`寰俊鏀粯鍥炶皟鏃堕棿鎴宠繃鏈?(${age}s)`);          return false;        }        // RSA-SHA256 骞冲彴璇佷功楠岀        const publicKeyPath = cfg.publicKeyPath;        if (publicKeyPath) {          try {            const publicKey = fs.readFileSync(publicKeyPath, 'utf-8');            // 楠岀瀛楃涓诧細timestamp\nnonce\nbody(JSON)\n            const bodyStr = JSON.stringify(body);            const verifyStr = `${timestamp}\n${nonce}\n${bodyStr}\n`;            const verify = crypto.createVerify('RSA-SHA256');            verify.update(verifyStr, 'utf8');            const isValid = verify.verify(              publicKey,              Buffer.from(signature, 'base64'),            );            if (!isValid) {              this.logger.error('寰俊鏀粯 V3 鍥炶皟 RSA-SHA256 楠岀澶辫触');              return false;            }            this.logger.log('寰俊鏀粯 V3 鍥炶皟 RSA-SHA256 楠岀閫氳繃');          } catch (err) {            this.logger.error(`寰俊鏀粯 V3 骞冲彴璇佷功楠岀寮傚父: ${(err as Error).message}`);            return false;          }        } else {          this.logger.warn('寰俊鏀粯骞冲彴璇佷功璺緞鏈厤缃紝璺宠繃 RSA-SHA256 楠岀锛堜笉瀹夊叏锛岃灏藉揩鍦ㄧ鐞嗗悗鍙伴厤缃級');        }        // AES-256-GCM 瑙ｅ瘑鍥炶皟瀵嗘枃        const apiV3Key = cfg.apiV3Key;        if (!apiV3Key) {          this.logger.error('WECHAT_API_V3_KEY 鏈厤缃紝鏃犳硶瑙ｅ瘑寰俊鏀粯 V3 鍥炶皟');          return false;        }        const resource = body.resource as Record<string, string>;        if (!resource || !resource.ciphertext || !resource.nonce) {          this.logger.error('寰俊鏀粯 V3 鍥炶皟缂哄皯 resource 瀛楁');          return false;        }        const ciphertextBuf = Buffer.from(resource.ciphertext, 'base64');        const authTag = ciphertextBuf.subarray(ciphertextBuf.length - 16);        const encryptedData = ciphertextBuf.subarray(0, ciphertextBuf.length - 16);        const decipher = crypto.createDecipheriv(          'aes-256-gcm',          Buffer.from(apiV3Key, 'utf-8'),          Buffer.from(resource.nonce, 'utf-8'),        );        decipher.setAuthTag(authTag);        const associatedData = resource.associated_data || '';        decipher.setAAD(Buffer.from(associatedData, 'utf-8'));        let decrypted: string;        try {          decrypted = Buffer.concat([            decipher.update(encryptedData),            decipher.final(),          ]).toString('utf8');        } catch (err) {          this.logger.error(            `寰俊鏀粯 V3 鍥炶皟 AES-256-GCM 瑙ｅ瘑澶辫触: ${(err as Error).message}`,          );          return false;        }        // 灏嗚В瀵嗗悗鐨勭湡瀹炰笟鍔℃暟鎹啓鍥?data锛屼緵 handlePaymentCallback 澶勭悊        const decryptedData = JSON.parse(decrypted) as Record<string, unknown>;        Object.assign(data, decryptedData);        return true;      } else if (channel === 'alipay') {        const cfg = await this.getAlipayConfig();        const sign = data.sign as string;        const signType = (data.sign_type as string) || 'RSA2';        if (!sign) return false;        if (!cfg.publicKey) {          this.logger.error('鏀粯瀹濆叕閽ユ湭閰嶇疆锛屾棤娉曢獙绛炬敮浠樺疂鍥炶皟');          return false;        }        // 鏀粯瀹濈鍚嶉獙璇侊細瀵瑰弬鏁版寜key鎺掑簭鎷兼帴鍚嶳SA楠岀        const sortedKeys = Object.keys(data)          .filter((k) => k !== 'sign' && k !== 'sign_type' && data[k] !== undefined && data[k] !== '')          .sort();        const signStr = sortedKeys          .map((k) => `${k}=${data[k]}`)          .join('&');        const verify = crypto.createVerify(          signType === 'RSA2' ? 'RSA-SHA256' : 'RSA-SHA1',        );        verify.update(signStr, 'utf8');        return verify.verify(          `-----BEGIN PUBLIC KEY-----\n${cfg.publicKey}\n-----END PUBLIC KEY-----`,          Buffer.from(sign, 'base64'),        );      } else if (channel === 'stripe') {        const cfg = await this.getStripeConfig();        const sig = data['stripe-signature'] as string;        if (!sig) return false;        const secret = cfg.webhookSecret;        if (!secret) {          this.logger.error('Stripe Webhook Secret 鏈厤缃紝鏃犳硶楠岀Stripe鍥炶皟');          return false;        }        // Stripe绛惧悕楠岃瘉锛欻MAC-SHA256        const payload = data.raw_body as string;        if (!payload) {          this.logger.error('Stripe鍥炶皟缂哄皯raw_body');          return false;        }        const elements = sig.split(',');        const timestampEl = elements.find((e) => e.startsWith('t='));        const v1El = elements.find((e) => e.startsWith('v1='));        if (!timestampEl || !v1El) return false;        const timestamp = timestampEl.slice(2);        const v1 = v1El.slice(3);        const signedPayload = `${timestamp}.${payload}`;        const expectedSig = crypto          .createHmac('sha256', secret)          .update(signedPayload)          .digest('hex');        // 闃查噸鏀撅細鏃堕棿鎴宠秴杩?鍒嗛挓鎷掔粷        const age = Math.floor(Date.now() / 1000) - Number(timestamp);        if (age > 300) {          this.logger.warn(`Stripe鍥炶皟鏃堕棿鎴宠繃鏈?(${age}s)`);          return false;        }        return crypto.timingSafeEqual(          Buffer.from(v1),          Buffer.from(expectedSig),        );      }      this.logger.warn(`鏈煡鏀粯娓犻亾: ${channel}`);      return false;    } catch (err) {      this.logger.error(`鏀粯楠岀寮傚父: ${(err as Error).message}`);      return false;    }  }  health() {    return { status: 'ok', module: 'payment' };  }}