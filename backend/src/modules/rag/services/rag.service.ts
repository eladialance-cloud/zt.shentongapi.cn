import {  HttpException,  HttpStatus,  Injectable,  Logger,  NotFoundException,} from '@nestjs/common';import { InjectRepository } from '@nestjs/typeorm';import { Repository, DataSource } from 'typeorm';import { createHash } from 'crypto';import { RedisService } from '../../../common/services/redis.service';import { KnowledgeBaseEntity } from '../../knowledge/entities/knowledge-base.entity';import { KnowledgeBaseDocumentEntity } from '../../knowledge/entities/knowledge-base-document.entity';import { KnowledgeBaseChunkEntity } from '../../knowledge/entities/knowledge-base-chunk.entity';/* ---------- DTO Interfaces ---------- */interface RetrieveQuery {  knowledgeBaseId: number;  query: string;  topK?: number;}export interface RetrieveResult {  query: string;  matches: ChunkMatchResult[];  message: string;  cached: boolean;}export interface ChunkMatchResult {  chunkId: number;  documentId: number;  documentName: string;  content: string;  chunkIndex: number;  score: number;}export interface AugmentResult {  augmentedPrompt: string;  contextCount: number;}interface IndexDocumentData {  knowledgeBaseId: number;  documentId: number;  content: string;  chunkSize?: number;  overlap?: number;}export interface IndexResult {  documentId: number;  knowledgeBaseId: number;  chunkCount: number;  totalTokens: number;}export interface ReindexResult {  knowledgeBaseId: number;  documentCount: number;  totalChunks: number;  totalTokens: number;}/* ---------- Service ---------- */@Injectable()export class RagService {  private readonly logger = new Logger(RagService.name);  constructor(    @InjectRepository(KnowledgeBaseEntity)    private readonly kbRepo: Repository<KnowledgeBaseEntity>,    @InjectRepository(KnowledgeBaseDocumentEntity)    private readonly docRepo: Repository<KnowledgeBaseDocumentEntity>,    @InjectRepository(KnowledgeBaseChunkEntity)    private readonly chunkRepo: Repository<KnowledgeBaseChunkEntity>,    private readonly redis: RedisService,    private readonly dataSource: DataSource,  ) {}  /* ---------- Health ---------- */  health() {    return { status: 'ok', module: 'rag' };  }  /* ---------- Retrieve ---------- */  async retrieve(userId: number, query: RetrieveQuery): Promise<RetrieveResult> {    const { knowledgeBaseId, query: queryText, topK = 5 } = query;    if (!queryText || !queryText.trim()) {      throw new HttpException('鏌ヨ鏂囨湰涓嶈兘涓虹┖', HttpStatus.BAD_REQUEST);    }    // 1. 楠岃瘉鐭ヨ瘑搴撳綊灞?    const kb = await this.kbRepo.findOne({      where: { id: knowledgeBaseId, userId },    });    if (!kb) {      throw new NotFoundException(`鐭ヨ瘑搴?${knowledgeBaseId} 涓嶅瓨鍦ㄦ垨鏃犳潈璁块棶`);    }    // 2. 妫€鏌?Redis 缂撳瓨    const cacheKey = this.buildCacheKey(userId, queryText, knowledgeBaseId);    const cached = await this.redis.get(cacheKey);    if (cached) {      const parsed = JSON.parse(cached) as RetrieveResult;      return { ...parsed, cached: true };    }    // 3. 浠?knowledge_base_chunks 琛ㄦ绱?    const matches = await this.searchChunks(knowledgeBaseId, queryText, topK);    const result: RetrieveResult = {      query: queryText,      matches,      message: matches.length > 0 ? `鎵惧埌 ${matches.length} 涓浉鍏虫枃妗ｇ墖娈礰 : '鏈壘鍒扮浉鍏虫枃妗ｇ墖娈?,      cached: false,    };    // 4. 鍐欏叆 Redis 缂撳瓨锛圱TL 60 绉掞級    try {      await this.redis.set(cacheKey, JSON.stringify(result), 60);    } catch (err) {      this.logger.warn(`缂撳瓨鍐欏叆澶辫触: ${(err as Error).message}`);    }    return result;  }  /* ---------- Augment Prompt ---------- */  async augmentPrompt(    userId: number,    data: { query: string; retrievedDocs: ChunkMatchResult[] },  ): Promise<AugmentResult> {    const { query, retrievedDocs } = data;    if (!query || !query.trim()) {      throw new HttpException('鏌ヨ鏂囨湰涓嶈兘涓虹┖', HttpStatus.BAD_REQUEST);    }    if (!retrievedDocs || retrievedDocs.length === 0) {      return {        augmentedPrompt: query,        contextCount: 0,      };    }    const contextBlocks = retrievedDocs.map((doc, index) => {      const title = doc.documentName || `鏂囨。${doc.documentId}`;      return `[鏂囨。${index + 1}] 鏍囬: ${title}\n鍐呭: ${doc.content}`;    });    const augmentedPrompt = [      '鍩轰簬浠ヤ笅鐭ヨ瘑搴撳唴瀹瑰洖绛旂敤鎴烽棶棰橈細',      '',      ...contextBlocks,      '',      `鐢ㄦ埛闂: ${query}`,    ].join('\n');    return {      augmentedPrompt,      contextCount: retrievedDocs.length,    };  }  /* ---------- Index Document ---------- */  async indexDocument(userId: number, data: IndexDocumentData): Promise<IndexResult> {    const {      knowledgeBaseId,      documentId,      content,      chunkSize = 500,      overlap = 50,    } = data;    if (!content || !content.trim()) {      throw new HttpException('鏂囨。鍐呭涓嶈兘涓虹┖', HttpStatus.BAD_REQUEST);    }    // 1. 楠岃瘉鐭ヨ瘑搴撳綊灞?    const kb = await this.kbRepo.findOne({      where: { id: knowledgeBaseId, userId },    });    if (!kb) {      throw new NotFoundException(`鐭ヨ瘑搴?${knowledgeBaseId} 涓嶅瓨鍦ㄦ垨鏃犳潈璁块棶`);    }    // 2. 楠岃瘉鏂囨。褰掑睘    const doc = await this.docRepo.findOne({      where: { id: documentId, knowledgeBaseId },    });    if (!doc) {      throw new NotFoundException(`鏂囨。 ${documentId} 涓嶅瓨鍦ㄤ簬鐭ヨ瘑搴?${knowledgeBaseId}`);    }    // 3. 鏂囨湰鍒嗙墖    const chunks = this.splitTextIntoChunks(content, chunkSize, overlap);    // 4. 浣跨敤浜嬪姟锛氬厛鍒犻櫎鏃у垎鐗囷紝鍐嶅啓鍏ユ柊鍒嗙墖    const result = await this.dataSource.transaction(async (manager) => {      // 鍒犻櫎璇ユ枃妗ｇ殑鏃у垎鐗?      await manager.delete(KnowledgeBaseChunkEntity, { documentId });      // 鏇存柊鏂囨。鐘舵€佷负 processing      await manager.update(        KnowledgeBaseDocumentEntity,        { id: documentId },        { status: 'processing' },      );      // 鍒涘缓鏂板垎鐗?      const chunkEntities: KnowledgeBaseChunkEntity[] = chunks.map((chunk, index) => {        const entity = new KnowledgeBaseChunkEntity();        entity.documentId = documentId;        entity.knowledgeBaseId = knowledgeBaseId;        entity.content = chunk.text;        entity.chunkIndex = index;        entity.tokenCount = chunk.tokenCount;        entity.embeddingId = `tfidf_${documentId}_${index}`;        return entity;      });      const savedChunks = await manager.save(        KnowledgeBaseChunkEntity,        chunkEntities,      );      // 鏇存柊鏂囨。鐘舵€佸拰缁熻      const totalTokens = chunks.reduce((sum, c) => sum + c.tokenCount, 0);      await manager.update(        KnowledgeBaseDocumentEntity,        { id: documentId },        {          status: 'done',          chunkCount: savedChunks.length,          tokenCount: totalTokens,        },      );      return {        chunkCount: savedChunks.length,        totalTokens,      };    });    // 5. 鏇存柊鐭ヨ瘑搴撶粺璁?    await this.updateKnowledgeBaseStats(knowledgeBaseId);    // 6. 娓呴櫎妫€绱㈢紦瀛?    await this.clearRetrieveCache(userId, knowledgeBaseId);    this.logger.log(      `鏂囨。 ${documentId} 绱㈠紩瀹屾垚: ${result.chunkCount} 涓垎鐗? ${result.totalTokens} tokens`,    );    return {      documentId,      knowledgeBaseId,      chunkCount: result.chunkCount,      totalTokens: result.totalTokens,    };  }  /* ---------- Reindex Knowledge Base ---------- */  async reindexKnowledgeBase(    userId: number,    knowledgeBaseId: number,  ): Promise<ReindexResult> {    // 1. 楠岃瘉鐭ヨ瘑搴撳綊灞?    const kb = await this.kbRepo.findOne({      where: { id: knowledgeBaseId, userId },    });    if (!kb) {      throw new NotFoundException(`鐭ヨ瘑搴?${knowledgeBaseId} 涓嶅瓨鍦ㄦ垨鏃犳潈璁块棶`);    }    // 2. 鏇存柊鐭ヨ瘑搴撶姸鎬佷负 reindexing    await this.kbRepo.update({ id: knowledgeBaseId }, { status: 'reindexing' });    try {      // 3. 鑾峰彇鎵€鏈夋枃妗?      const documents = await this.docRepo.find({        where: { knowledgeBaseId },      });      if (documents.length === 0) {        await this.kbRepo.update(          { id: knowledgeBaseId },          { status: 'active', documentCount: 0, totalChunks: 0, totalTokens: 0 },        );        return {          knowledgeBaseId,          documentCount: 0,          totalChunks: 0,          totalTokens: 0,        };      }      let totalChunks = 0;      let totalTokens = 0;      // 4. 閫愪釜鏂囨。閲嶆柊绱㈠紩      for (const doc of documents) {        // 璇诲彇鏂囨。鍐呭锛堣繖閲屼粠鏂囦欢绯荤粺璇诲彇鎴栦娇鐢ㄥ凡瀛樺偍鍐呭锛?        // 鐢变簬 filePath 瀛樺偍鐨勬槸鏂囦欢璺緞锛屾垜浠皾璇曡鍙?        const content = await this.readDocumentContent(doc);        if (!content || !content.trim()) {          this.logger.warn(`鏂囨。 ${doc.id} 鍐呭涓虹┖锛岃烦杩嘸);          continue;        }        const chunks = this.splitTextIntoChunks(          content,          kb.chunkSize || 500,          kb.chunkOverlap || 50,        );        await this.dataSource.transaction(async (manager) => {          // 鍒犻櫎鏃у垎鐗?          await manager.delete(KnowledgeBaseChunkEntity, { documentId: doc.id });          // 鍒涘缓鏂板垎鐗?          const chunkEntities: KnowledgeBaseChunkEntity[] = chunks.map(            (chunk, index) => {              const entity = new KnowledgeBaseChunkEntity();              entity.documentId = doc.id;              entity.knowledgeBaseId = knowledgeBaseId;              entity.content = chunk.text;              entity.chunkIndex = index;              entity.tokenCount = chunk.tokenCount;              entity.embeddingId = `tfidf_${doc.id}_${index}`;              return entity;            },          );          const saved = await manager.save(            KnowledgeBaseChunkEntity,            chunkEntities,          );          const docTokens = chunks.reduce((sum, c) => sum + c.tokenCount, 0);          await manager.update(            KnowledgeBaseDocumentEntity,            { id: doc.id },            {              status: 'done',              chunkCount: saved.length,              tokenCount: docTokens,            },          );          totalChunks += saved.length;          totalTokens += docTokens;        });      }      // 5. 鏇存柊鐭ヨ瘑搴撶粺璁″拰鐘舵€?      await this.kbRepo.update(        { id: knowledgeBaseId },        {          status: 'active',          documentCount: documents.length,          totalChunks,          totalTokens,        },      );      // 6. 娓呴櫎妫€绱㈢紦瀛?      await this.clearRetrieveCache(userId, knowledgeBaseId);      this.logger.log(        `鐭ヨ瘑搴?${knowledgeBaseId} 閲嶆柊绱㈠紩瀹屾垚: ${documents.length} 鏂囨。, ${totalChunks} 鍒嗙墖`,      );      return {        knowledgeBaseId,        documentCount: documents.length,        totalChunks,        totalTokens,      };    } catch (err) {      // 閿欒鏃舵仮澶嶇姸鎬?      await this.kbRepo.update({ id: knowledgeBaseId }, { status: 'error' });      this.logger.error(        `鐭ヨ瘑搴?${knowledgeBaseId} 閲嶆柊绱㈠紩澶辫触: ${(err as Error).message}`,      );      throw new HttpException(        `閲嶆柊绱㈠紩澶辫触: ${(err as Error).message}`,        HttpStatus.INTERNAL_SERVER_ERROR,      );    }  }  /* ---------- Private Methods ---------- */  /**   * 鍦ㄧ煡璇嗗簱鐨勫垎鐗囦腑鎼滅储鍖归厤鍐呭   * 浣跨敤 MySQL FULLTEXT 鎼滅储锛屽洖閫€鍒?LIKE 妯＄硦鍖归厤   */  private async searchChunks(    knowledgeBaseId: number,    queryText: string,    topK: number,  ): Promise<ChunkMatchResult[]> {    // 鎻愬彇鍏抽敭璇?    const keywords = this.extractKeywords(queryText);    if (keywords.length === 0) {      return [];    }    // 鏂规 1: 灏濊瘯 FULLTEXT 鎼滅储    try {      const fulltextResults = await this.fulltextSearch(        knowledgeBaseId,        queryText,        topK,      );      if (fulltextResults.length > 0) {        return fulltextResults;      }    } catch (err) {      this.logger.warn(        `FULLTEXT 鎼滅储澶辫触锛屽洖閫€鍒?LIKE 鍖归厤: ${(err as Error).message}`,      );    }    // 鏂规 2: LIKE 妯＄硦鍖归厤 + 鍏抽敭璇嶈瘎鍒?    return this.likeSearch(knowledgeBaseId, keywords, topK);  }  /**   * FULLTEXT 鎼滅储   */  private async fulltextSearch(    knowledgeBaseId: number,    queryText: string,    topK: number,  ): Promise<ChunkMatchResult[]> {    // 浣跨敤鍙傛暟鍖栨煡璇㈤伩鍏?SQL 娉ㄥ叆    // FULLTEXT 鎼滅储闇€瑕佷娇鐢?NATURAL LANGUAGE MODE 鎴?BOOLEAN MODE    const escapedQuery = queryText.replace(/['"\\]/g, ' ').trim();    const rawResults = await this.chunkRepo      .createQueryBuilder('chunk')      .leftJoinAndSelect(        KnowledgeBaseDocumentEntity,        'doc',        'doc.id = chunk.documentId',      )      .select([        'chunk.id AS chunkId',        'chunk.document_id AS documentId',        'doc.name AS documentName',        'chunk.content AS content',        'chunk.chunk_index AS chunkIndex',      ])      .addSelect(        `MATCH(chunk.content) AGAINST (:queryText IN NATURAL LANGUAGE MODE)`,        'score',      )      .where('chunk.knowledge_base_id = :kbId', { kbId: knowledgeBaseId })      .andWhere(`MATCH(chunk.content) AGAINST (:queryText IN NATURAL LANGUAGE MODE)`)      .setParameters({ queryText: escapedQuery })      .orderBy('score', 'DESC')      .limit(topK)      .getRawMany<{        chunkId: string;        documentId: string;        documentName: string;        content: string;        chunkIndex: number;        score: number;      }>();    return rawResults.map((row) => ({      chunkId: Number(row.chunkId),      documentId: Number(row.documentId),      documentName: row.documentName || '',      content: row.content,      chunkIndex: row.chunkIndex,      score: Number(row.score) || 0,    }));  }  /**   * LIKE 妯＄硦鍖归厤 + 鍏抽敭璇嶈瘎鍒?   * 璇勫垎閫昏緫锛氭瘡涓叧閿瘝鍦ㄧ墖娈典腑鍑虹幇鐨勬鏁颁箣鍜?   */  private async likeSearch(    knowledgeBaseId: number,    keywords: string[],    topK: number,  ): Promise<ChunkMatchResult[]> {    // 鏋勫缓鏌ヨ锛氫换涓€鍏抽敭璇嶅尮閰嶇殑鐗囨    const qb = this.chunkRepo      .createQueryBuilder('chunk')      .leftJoinAndSelect(        KnowledgeBaseDocumentEntity,        'doc',        'doc.id = chunk.documentId',      )      .select([        'chunk.id AS chunkId',        'chunk.document_id AS documentId',        'doc.name AS documentName',        'chunk.content AS content',        'chunk.chunk_index AS chunkIndex',      ])      .where('chunk.knowledge_base_id = :kbId', { kbId: knowledgeBaseId })      .andWhere((qb) => {        const conditions: string[] = [];        keywords.forEach((kw, i) => {          conditions.push(`chunk.content LIKE :kw${i}`);          qb.setParameter(`kw${i}`, `%${kw}%`);        });        return conditions.join(' OR ');      })      .limit(topK * 3); // 澶氬彇涓€浜涚敤浜庤瘎鍒嗘帓搴?    const rawResults = await qb.getRawMany<{      chunkId: string;      documentId: string;      documentName: string;      content: string;      chunkIndex: number;    }>();    // 璁＄畻姣忎釜鐗囨鐨勮瘎鍒?    const scored = rawResults.map((row) => {      const contentLower = row.content.toLowerCase();      let score = 0;      for (const kw of keywords) {        const kwLower = kw.toLowerCase();        // 缁熻鍏抽敭璇嶅嚭鐜版鏁?        let idx = contentLower.indexOf(kwLower);        while (idx !== -1) {          score += 1;          idx = contentLower.indexOf(kwLower, idx + kwLower.length);        }      }      return {        chunkId: Number(row.chunkId),        documentId: Number(row.documentId),        documentName: row.documentName || '',        content: row.content,        chunkIndex: row.chunkIndex,        score,      };    });    // 鎸夎瘎鍒嗛檷搴忔帓搴忥紝鍙?topK    scored.sort((a, b) => b.score - a.score);    return scored.slice(0, topK);  }  /**   * 浠庢煡璇㈡枃鏈腑鎻愬彇鍏抽敭璇?   * 绠€鍗曞疄鐜帮細鎸夌┖鏍煎拰鏍囩偣鍒嗚瘝锛岃繃婊ゅ仠鐢ㄨ瘝鍜岃繃鐭殑璇?   */  private extractKeywords(query: string): string[] {    // 涓枃鍜岃嫳鏂囨贩鍚堝垎璇?    // 鎸夐潪瀛楁瘝鏁板瓧鍜屼腑鏂囧瓧绗﹀垎鍓?    const tokens = query      .replace(/[^\u4e00-\u9fa5a-zA-Z0-9\s]/g, ' ')      .split(/\s+/)      .filter((t) => t.length >= 2);    // 鍋滅敤璇嶅垪琛?    const stopWords = new Set([      '鐨?, '浜?, '鍦?, '鏄?, '鎴?, '鏈?, '鍜?, '灏?, '涓?, '浜?, '閮?, '涓€',      '涓€涓?, '涓?, '涔?, '寰?, '鍒?, '璇?, '瑕?, '鍘?, '浣?, '浼?, '鐫€', '娌℃湁',      '鐪?, '濂?, '鑷繁', '杩?, '閭?, '瀹?, '浠?, '濂?, '浠?, '浠€涔?, '鎬庝箞',      'the', 'a', 'an', 'is', 'are', 'was', 'were', 'be', 'been', 'being',      'have', 'has', 'had', 'do', 'does', 'did', 'will', 'would', 'could',      'should', 'may', 'might', 'must', 'shall', 'can', 'need', 'and', 'or',      'but', 'if', 'then', 'else', 'when', 'where', 'why', 'how', 'what',      'which', 'who', 'whom', 'this', 'that', 'these', 'those', 'in', 'on',      'at', 'to', 'for', 'of', 'with', 'by', 'from', 'as', 'it', 'its',    ]);    return tokens.filter((t) => !stopWords.has(t.toLowerCase()));  }  /**   * 鏂囨湰鍒嗙墖锛堟寜瀛楃鏁板垏鍓诧紝甯?overlap 閲嶅彔锛?   */  private splitTextIntoChunks(    text: string,    chunkSize: number,    overlap: number,  ): { text: string; tokenCount: number }[] {    const chunks: { text: string; tokenCount: number }[] = [];    if (!text || text.length === 0) {      return chunks;    }    // 纭繚 overlap < chunkSize    const effectiveOverlap = Math.min(overlap, chunkSize - 1);    const step = chunkSize - effectiveOverlap;    let start = 0;    while (start < text.length) {      const end = Math.min(start + chunkSize, text.length);      const chunkText = text.slice(start, end).trim();      if (chunkText.length > 0) {        // 绠€鍗曚及绠?token 鏁帮細涓枃绾?1 瀛?= 1 token锛岃嫳鏂囩害 4 瀛楃 = 1 token        const tokenCount = this.estimateTokenCount(chunkText);        chunks.push({ text: chunkText, tokenCount });      }      if (end >= text.length) {        break;      }      start += step;    }    return chunks;  }  /**   * 绠€鍗曚及绠?token 鏁?   * 涓枃锛氱害 1 瀛?= 1.5 tokens   * 鑻辨枃锛氱害 4 瀛楃 = 1 token   */  private estimateTokenCount(text: string): number {    let count = 0;    for (const char of text) {      if (/[\u4e00-\u9fa5]/.test(char)) {        count += 1.5;      } else {        count += 0.25;      }    }    return Math.ceil(count);  }  /**   * 鏇存柊鐭ヨ瘑搴撶粺璁℃暟鎹?   */  private async updateKnowledgeBaseStats(knowledgeBaseId: number): Promise<void> {    const stats = await this.chunkRepo      .createQueryBuilder('chunk')      .select('COUNT(*)', 'totalChunks')      .addSelect('COALESCE(SUM(chunk.token_count), 0)', 'totalTokens')      .where('chunk.knowledge_base_id = :kbId', { kbId: knowledgeBaseId })      .getRawOne<{ totalChunks: string; totalTokens: string }>();    const docCount = await this.docRepo.count({      where: { knowledgeBaseId },    });    await this.kbRepo.update(      { id: knowledgeBaseId },      {        documentCount: docCount,        totalChunks: Number(stats?.totalChunks || 0),        totalTokens: Number(stats?.totalTokens || 0),        status: 'active',      },    );  }  /**   * 璇诲彇鏂囨。鍐呭   */  private async readDocumentContent(    doc: KnowledgeBaseDocumentEntity,  ): Promise<string> {    try {      const fs = await import('fs/promises');      const path = await import('path');      const filePath = path.resolve(doc.filePath);      // 浠呭厑璁歌鍙栨枃浠讹紝涓嶅仛閫掑綊澶勭悊      const content = await fs.readFile(filePath, 'utf-8');      return content;    } catch (err) {      this.logger.warn(        `璇诲彇鏂囨。 ${doc.id} 鍐呭澶辫触: ${(err as Error).message}`,      );      return '';    }  }  /**   * 鏋勫缓 Redis 缂撳瓨 key   */  private buildCacheKey(    userId: number,    query: string,    knowledgeBaseId: number,  ): string {    const queryHash = createHash('md5').update(query).digest('hex');    return `rag:retrieve:${userId}:${queryHash}:${knowledgeBaseId}`;  }  /**   * 娓呴櫎鎸囧畾鐭ヨ瘑搴撶殑妫€绱㈢紦瀛?   * 浣跨敤 SCAN 鏌ユ壘鍖归厤鐨?key 骞跺垹闄?   */  private async clearRetrieveCache(    userId: number,    knowledgeBaseId: number,  ): Promise<void> {    try {      const client = this.redis.getClient();      const pattern = `rag:retrieve:${userId}:*:${knowledgeBaseId}`;      let cursor = '0';      do {        const [nextCursor, keys] = await client.scan(          cursor,          'MATCH',          pattern,          'COUNT',          100,        );        cursor = nextCursor;        if (keys.length > 0) {          await client.del(...keys);        }      } while (cursor !== '0');    } catch (err) {      this.logger.warn(        `娓呴櫎妫€绱㈢紦瀛樺け璐? ${(err as Error).message}`,      );    }  }}