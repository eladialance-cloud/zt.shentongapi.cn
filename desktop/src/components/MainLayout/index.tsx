/** * MainLayout 鈥?v1.0 鐢ㄦ埛妗岄潰涓诲竷灞€ * 缁撴瀯: TopBar(48px) + [IconDock(48px) + Content] + StatusBar(28px) * 鈱楰 鍛戒护闈㈡澘鍏ㄥ眬鎸傝浇 */import { useCallback, useState } from 'react'import { Outlet } from 'react-router-dom'import TopBar from './TopBar'import IconDock from '@/components/IconDock'import CommandPalette from '@/components/CommandPalette'import StatusBar from './StatusBar'import Breadcrumb from '@/components/Breadcrumb'import styles from './styles.module.css'export default function MainLayout() {  const [searchOpen, setSearchOpen] = useState(false)  const openSearch = useCallback(() => setSearchOpen(true), [])  const closeSearch = useCallback(() => setSearchOpen(false), [])  return (    <div className={styles.layout}>      <TopBar onOpenSearch={openSearch} />      <div className={styles.body}>        <IconDock />        <main className={styles.content}>          <Breadcrumb />          <Outlet />        </main>      </div>      <StatusBar />      <CommandPalette open={searchOpen} onClose={closeSearch} />    </div>  )}