# 生产环境配置模板（真实配置不纳入版本控制）
VITE_API_BASE_URL=https://your-production-domain/api
VITE_WS_URL=wss://your-production-domain
# 渲染层 CSP connect-src 的追加项（空格分隔来源表达式）。
# 默认来源见 src/index.html（本地服务 + *.shentongapi.cn）。
# 【自建部署必读】若把 API 放在自有域名，必须在这里补上，否则渲染层会被 CSP 拦下、
# 表现为「所有接口请求失败」。例如：
#   VITE_CSP_CONNECT_EXTRA=https://your-production-domain wss://your-production-domain
# 注：该项必须存在（可为空），否则 Vite 不会替换 index.html 里的 %VITE_CSP_CONNECT_EXTRA%。
VITE_CSP_CONNECT_EXTRA=
